//
//  ViewController.swift
//  Chess pre-test
//
//  Created by JPLee on 11/13/16.
//  Copyright © 2016 JPLee. All rights reserved.
//

import UIKit

let sw = UIScreen.main.bounds.width
let sh = UIScreen.main.bounds.height
let unit = sw/8

class ViewController: UIViewController {

    @IBAction func toPVP(sender: UIButton) {
    }
    
    @IBAction func toAI(sender: UIButton) {
    }
    @IBAction func toTutorial(sender: AnyObject) {
    }
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }


}

/*
 if switch on for timer {
 go look at the following buttons
    -> default is the left one
 }
 or 
 look at those buttons and whichever is clicked on is the one that will activate.
 layout later with description
 consider that the design is really important
 */
